<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liyonglin.accounts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyStyle">
        <activity android:name=".activity.WelcomeActivity"></activity>
        <activity android:name=".activity.HomepageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ToolBarActivity" />
        <activity android:name=".activity.AccountAddActivity" />
        <activity android:name=".activity.ClassifyListActivity" />
        <activity android:name=".activity.ClassifyUpdateActivity"/>
        <activity android:name=".activity.ClassifyAddActivity"/>
    </application>

</manifest>